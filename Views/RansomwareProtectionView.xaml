<Page x:Class="SkidrowKiller.Views.RansomwareProtectionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Ransomware Protection" FontSize="28" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
                <Border x:Name="StatusBadge" CornerRadius="4" Padding="8,4" Margin="12,0,0,0"
                        VerticalAlignment="Center" Background="{StaticResource SuccessBrush}">
                    <TextBlock x:Name="StatusText" Text="Active" FontSize="11" FontWeight="SemiBold" Foreground="White"/>
                </Border>
            </StackPanel>
            <TextBlock Text="Protect your important files from encryption attacks using honeypot detection and behavioral analysis"
                       FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Quick Stats -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Protected Folders -->
            <Border Grid.Column="0" Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Protected" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="TxtFolderCount" Text="0" FontSize="28" FontWeight="Bold"
                               Foreground="{StaticResource AccentPrimaryBrush}" Margin="0,4,0,0"/>
                    <TextBlock Text="Folders" FontSize="11" Foreground="{StaticResource TextTertiaryBrush}"/>
                </StackPanel>
            </Border>

            <!-- Honeypots -->
            <Border Grid.Column="1" Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel>
                    <TextBlock Text="Honeypots" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="TxtHoneypotCount" Text="0" FontSize="28" FontWeight="Bold"
                               Foreground="{StaticResource WarningBrush}" Margin="0,4,0,0"/>
                    <TextBlock Text="Active Traps" FontSize="11" Foreground="{StaticResource TextTertiaryBrush}"/>
                </StackPanel>
            </Border>

            <!-- Files Monitored -->
            <Border Grid.Column="2" Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel>
                    <TextBlock Text="Monitored" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="TxtFilesMonitored" Text="0" FontSize="28" FontWeight="Bold"
                               Foreground="{StaticResource SuccessBrush}" Margin="0,4,0,0"/>
                    <TextBlock Text="File Snapshots" FontSize="11" Foreground="{StaticResource TextTertiaryBrush}"/>
                </StackPanel>
            </Border>

            <!-- Alerts -->
            <Border Grid.Column="3" Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="Alerts" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="TxtAlertCount" Text="0" FontSize="28" FontWeight="Bold"
                               Foreground="{StaticResource DangerBrush}" Margin="0,4,0,0"/>
                    <TextBlock Text="Triggered" FontSize="11" Foreground="{StaticResource TextTertiaryBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Protected Folders Section -->
        <Border Grid.Row="2" Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Protected Folders" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                    <Button x:Name="BtnAddFolder" Grid.Column="1" Content="+ Add Folder"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="BtnAddFolder_Click" Padding="12,6"/>
                </Grid>

                <!-- Folders List -->
                <ItemsControl x:Name="FoldersList" Grid.Row="1">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Grid>
        </Border>

        <!-- Activity Log & Alerts -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Alerts Panel -->
            <Border Grid.Column="0" Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="Security Alerts" FontSize="16" FontWeight="SemiBold"
                                   Foreground="{StaticResource DangerBrush}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                                  Style="{StaticResource ModernScrollViewerStyle}">
                        <StackPanel x:Name="AlertsPanel">
                            <TextBlock x:Name="EmptyAlertsText" Text="No alerts. Your files are safe."
                                       FontSize="12" Foreground="{StaticResource SuccessBrush}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,20"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Activity Log -->
            <Border Grid.Column="1" Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="Activity Log" FontSize="16" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                        <Button x:Name="BtnClearLog" Content="Clear"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Click="BtnClearLog_Click" Padding="12,4" Margin="12,0,0,0"
                                FontSize="11"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                                  Style="{StaticResource ModernScrollViewerStyle}">
                        <StackPanel x:Name="LogPanel">
                            <TextBlock x:Name="EmptyLogText" Text="No activity yet."
                                       FontSize="12" Foreground="{StaticResource TextTertiaryBrush}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,20"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Actions -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="BtnToggleProtection" Content="Stop Protection"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Click="BtnToggleProtection_Click" Width="140"/>
            <Button x:Name="BtnRefreshSnapshots" Content="Refresh Snapshots"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="BtnRefreshSnapshots_Click" Width="160" Margin="12,0,0,0"/>
        </StackPanel>
    </Grid>
</Page>
